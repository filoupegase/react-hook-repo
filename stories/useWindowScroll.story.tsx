import * as React from 'react';
import { useWindowScroll } from "../src";

// TODO : Coming soon
const Demo = () => {
    //const { x, y } = useWindowScroll();

    return (
        <div></div>
    );
};
